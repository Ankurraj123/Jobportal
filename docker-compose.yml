version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
    volumes:
      - .:/app
    # Run runserver locally for reload support, or default to Dockerfile CMD
    command: python manage.py runserver 0.0.0.0:8000
